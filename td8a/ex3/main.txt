#include <stdio.h>

// factorial
int factorial(int n) {
  if (n <= 0)
    return 1;
  else
    return n * factorial(n - 1);
}

int main(void) {
  printf("%d\n", factorial(3));
  printf("%d\n", factorial(5));
  printf("%d\n", factorial(10));
  return 0;
}