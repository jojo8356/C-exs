#include <stdbool.h>

#pragma once

int decrease(int);
int increase(int);
bool greaterThan(int, int);
bool lowerThan(int, int);
bool equalTo(int, int);
bool isMultiple(int a, int b);
int absValue(int nb);